{
  "name": "accessibility-review-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "A local Figma plugin for generating accessible design review workspaces.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/saikrishnapachala/Accessibility-Review-Assistant-Figma-Plugin.git"
  },
  "bugs": {
    "url": "https://github.com/saikrishnapachala/Accessibility-Review-Assistant-Figma-Plugin/issues"
  },
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run typecheck && npm run build && npm run test",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --project tsconfig.plugin.json --noEmit && tsc --project tsconfig.ui.json --noEmit && tsc --project tsconfig.test.json --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.132.0",
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=22"
  }
}
